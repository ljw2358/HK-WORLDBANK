const express = require('express');
const mongoose = require('mongoose');
const app = express();
app.use(express.json());

// MongoDB 연결
mongoose.connect(process.env.MONGODB_URI || 'mongodb+srv://ljw2358:<db_password>@cluster0.yz8wdjb.mongodb.net/?retryWrites=true&w=majority');

const Asset = mongoose.model('Asset', { wallet: String, balance: Number });

app.get('/api/assets', async (req, res) => {
    const data = await Asset.findOne({ wallet: "HK_MAIN_NODE" }) || { wallet: "HK_MAIN_NODE", balance: 0.00 };
    res.json(data);
});

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => console.log(`HK 월드뱅크 서버 가동 중 (Port: ${PORT})`));


